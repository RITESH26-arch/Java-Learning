public class Rectangle {
    int length;
    int width;

    Rectangle(int length,int width){
        this.length = length;
        this.width = width;
    }

    void Display(){
        System.out.println("The Length of the Rectangle is " + length + " and the width of the rectangle is " + width);
    }
}
